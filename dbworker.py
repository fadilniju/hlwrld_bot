import config
import postgresql


"""
def get_curr_state(user_id):
    with postgresql.open(config.DB_URL) as db:
        query = db.query("SELECT * FROM users_state WHERE id = " )
        try:
            return db
"""


